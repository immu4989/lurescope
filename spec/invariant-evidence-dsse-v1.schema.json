{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/immu4989/lurescope/spec/invariant-evidence-dsse/v1",
  "title": "LureScope LureInvariant evidence DSSE envelope v1",
  "description": "DSSE envelope carrying exact LureInvariant checkpoint statement bytes.",
  "type": "object",
  "additionalProperties": false,
  "required": ["payloadType", "payload", "signatures"],
  "properties": {
    "payloadType": {"const": "application/vnd.in-toto+json"},
    "payload": {"type": "string", "contentEncoding": "base64", "minLength": 4},
    "signatures": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["keyid", "sig"],
        "properties": {
          "keyid": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
          "sig": {"type": "string", "contentEncoding": "base64", "minLength": 8}
        }
      }
    }
  }
}
