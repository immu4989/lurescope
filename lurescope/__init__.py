"""LureScope — deployable fraud-lure scoring API with a live adversarial demo.

The serving companion to LureBench: it wraps LureBench's detectors behind a small
FastAPI service so you can score a single message and watch an attacker try to evade
the detector in real time.
"""

__version__ = "0.1.0"
